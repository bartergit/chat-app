{
    "name": "svelte-app",
    "version": "1.0.0",
    "scripts": {
        "heroku-prebuild": "cd app/client && npm install && npm install --global rollup && npm run build"
    },
    "engines": {
    "node": "12.x"
  }
}
